# 九宫格抽奖额度安全审查

审查范围：`controller/lottery.go`、`service/lottery.go`、`model/lottery.go`、抽奖路由及 `web/src/features/lottery/`、管理员奖项配置页面。

结论：**当前实现不能认为已经具备防止额度被刷乱的安全性。** 同一账号的重复抽奖有数据库唯一索引兜底，正常并发请求不会直接造成同一账号重复发奖；但奖池的经济模型、批量账号滥用、额度边界和配置约束仍存在高风险。

## 高风险问题

### 1. 可通过批量账号每天重复领取，且没有总量或奖项库存限制

**等级：严重（P0）**

`DrawLottery` 只根据 `user_id + draw_day` 限制次数，数据库唯一索引也是同样的维度（`model/lottery.go:35-44`、`service/lottery.go:79-87`）。抽奖接口只使用 `UserAuth()`，没有抽奖专用的用户/IP/设备限流、账号年龄限制、邮箱验证要求、Turnstile 或人工风控（`router/api-router.go:41-50`）。

奖项没有每日发放数量、奖池预算或总预算字段；只要注册新账号，就能再次获得一次抽奖机会。攻击者可以批量注册账号并持续抽奖，额度支出没有系统级上限。

**建议：**

- 在服务端增加奖池预算或每个奖项的每日库存，并在同一事务内扣减库存。
- 抽奖资格不能只依赖用户 ID，至少增加注册时长、邮箱/手机验证、风控状态和专用限流；必要时增加 IP/设备/支付或邀请来源限制。
- 对高价值奖项设置每日总额上限，达到上限后自动切换为零额度奖项或停止抽奖。
- 把预期每日发放额度作为上线前的硬性预算指标，而不是只看单用户每日次数。

### 2. 默认奖项的额度非常高，预期每次抽奖约发放 43 美元额度

**等级：严重（P0，需确认业务意图）**

默认奖项使用 `defaultQuotaForUsd(500/300/200/100/50/30/10)`（`model/lottery.go:51-59`），该函数会乘以 `common.QuotaPerUnit`（`model/lottery.go:62-66`）。这意味着默认一等奖实际是 500 美元额度，而不是 500 个内部积分；按默认权重计算，单次抽奖的数学期望为：

```text
(1*500 + 2*300 + 4*200 + 8*100 + 15*50 + 20*30 + 25*10) / 100 = 43 美元
```

如果产品原意是“500 积分”，这里会产生约 `QuotaPerUnit` 倍的超额发放；即使产品确实以美元额度为单位，P0 的批量账号问题也会让攻击者以约 43 美元/账号/天的期望成本消耗额度。

**建议：** 明确奖项配置使用“内部 quota 单位”还是“美元金额”，统一字段命名和转换入口；上线前给出最大单次奖励、单用户日奖励、全站日预算三层硬上限，并为默认配置增加单元测试锁定金额。

### 3. 奖励额度没有使用统一的饱和/严格边界，可能导致跨数据库溢出或不一致

**等级：高（P1）**

管理员提交的 `quota_amount` 只校验 `>= 0`，没有限制到 `common.MaxQuota`，也没有检查“当前用户额度 + 奖励额度”是否仍在可表示范围内（`service/lottery.go:193-215`）。实际发放使用原始整数表达式 `quota + ?`（`model/lottery.go:139-143`），没有使用额度安全规范要求的 checked/strict 转换或溢出保护。

后果取决于数据库：MySQL/PostgreSQL 可能因 `int` 溢出回滚，SQLite 可能接受超出 32 位列约定的值，造成三种数据库行为不一致；接近边界的用户还可能在后续额度运算中发生异常。管理员接口虽然受 `AdminAuth()` 保护，但配置接口是直接可调用的高价值写入口，不能只依赖前端输入框的 `min=0`。

**建议：**

- 服务端拒绝 `quota_amount <= 0` 之外的超大值，使用 `common.MaxQuota` 作为上限。
- 发放前以数据库条件更新或事务内读取并检查 `quota <= MaxQuota - award`，超限时整笔抽奖回滚。
- 奖励计算、记录快照、日志和缓存同步使用同一安全转换结果；不要在该路径重新引入裸整数加法。
- 增加 SQLite、MySQL、PostgreSQL 的边界行为测试。

### 4. 事务没有检查用户额度更新的实际影响行数

**等级：中高（P1）**

`CreateLotteryDrawRecordTx` 插入抽奖记录后执行用户额度更新，但只检查 `Error`，不检查 `RowsAffected`（`model/lottery.go:136-145`）。如果用户在认证后被软删除、删除或发生异常状态变化，更新可能影响 0 行；事务仍会提交抽奖记录，接口返回中奖结果，但用户并未收到额度。

这会造成“抽奖记录显示已发奖、账户实际未发奖”的账务不一致，也会让后续人工补偿缺少明确依据。

**建议：** 当 `quotaToAdd > 0` 时要求 `RowsAffected == 1`，否则返回 `gorm.ErrRecordNotFound` 并回滚；同时在发奖事务中固定用户存在且未删除的条件。

## 中风险问题

### 5. 并发冲突没有映射为“今日已抽奖”

**等级：中（P2）**

事务内的 count 检查不能消除两个请求同时读到 0 的情况，最终依靠唯一索引阻止第二次插入，这是正确的重复发奖保护。但 `CreateLotteryDrawRecordTx` 直接返回数据库唯一约束错误（`model/lottery.go:126-149`），没有像旧的 `CreateLotteryDrawRecord` 那样转换为 `ErrLotteryDailyLimitReached`（`model/lottery.go:105-115`）。

第二个并发请求会被控制器当作普通服务器错误，前端显示“服务暂时不可用”，而不是明确告知用户已经抽过。额度不会因此重复增加，但会造成错误重试、客服争议和不必要的压力。

**建议：** 对事务插入的唯一键冲突做跨数据库可识别的错误映射，返回 HTTP 409 和 `LOTTERY_DAILY_LIMIT_REACHED`；不要依赖具体数据库错误文本。

### 6. 奖项位置没有完整性约束，服务端结果可能与前端展示不一致

**等级：中（P2）**

`SaveLotteryPrize` 只要求 `sort_order > 0`，没有限制在 1-8，也没有保证唯一（`service/lottery.go:202-210`）。管理员可以配置重复位置、超过 8 的位置或超过 8 个有效奖项。

服务端返回 `selected.SortOrder - 1`（`service/lottery.go:135-143`），而前端固定渲染 8 格，并用 `prizes.length` 计算动画路径、再将结果位置强行 clamp（`web/src/features/lottery/index.tsx:63-70`、`web/src/features/lottery/index.tsx:91-104`、`web/src/features/lottery/components/lottery-board.tsx:39-54`）。在异常配置下，用户看到的高亮格可能不是实际中奖奖项；额度已按服务端奖项发放，但界面显示另一奖项。

**建议：** 服务端强制有效奖项数量不超过 8、`sort_order` 必须为 1-8 且唯一；抽奖返回前校验选中奖项位置；前端按服务端完整配置建立索引，不要用奖项数量替代固定棋盘大小。

### 7. 抽奖奖励日志不在发奖事务内，失败只记录系统日志

**等级：中（P2）**

额度事务提交后才调用 `model.RecordLog`（`service/lottery.go:130-133`）。日志写入失败不会影响已提交的额度，造成“已发额度但没有充值日志”的审计缺口。对于可被批量滥用的活动，缺少可靠的发放审计会显著增加追责和回滚难度。

**建议：** 将奖励流水设计为与抽奖记录同事务写入的不可变业务流水，并让日志展示从该流水生成；至少为日志失败增加可重试的持久化补偿，而不是只写进程日志。

## 前端审查结果

- `drawRequestActive` 和 React Query mutation 能防止同一页面的重复点击（`web/src/features/lottery/index.tsx:42-44`、`web/src/features/lottery/index.tsx:150-156`），但这只是用户体验保护，不能替代服务端幂等和反滥用控制。
- 前端展示的 `remaining`、`boardIndex`、`quotaAmount` 都来自服务端，攻击者修改前端状态不会直接改变发奖金额；这一点是正确的。
- 管理员页面的 `min=0`、`canSave` 仅是客户端校验，任何人都可以绕过；额度上限必须在后端强制执行（`web/src/features/system-settings/maintenance/lottery-prizes-section.tsx:90-111`、`web/src/features/system-settings/maintenance/lottery-prizes-section.tsx:214-236`）。
- “我的历史”使用数组下标作为 React key（`web/src/features/lottery/components/records-panel.tsx:191-198`），会在记录刷新/排序变化时复用错误的行状态；当前行没有本地状态，暂不构成额度漏洞，但建议改用稳定的记录 ID。这个接口目前没有返回历史记录 ID，需要后端补充。
- 全部中奖记录公开返回 `DisplayName`，控制器直接读取用户名（`controller/lottery.go:15-23`、`service/lottery.go:152-161`）；模型注释称其为“脱敏用户名”，实际没有脱敏。不是额度漏洞，但存在用户隐私暴露问题，建议服务端统一掩码。

## 已有保护

- 抽奖请求不接受客户端奖项、权重或额度参数，奖项由服务端数据库读取并使用 `crypto/rand` 选择（`service/lottery.go:89-111`）。
- `draw_records` 有 `(user_id, draw_day)` 唯一索引，数据库层能阻止同一账号同一天重复落库（`model/lottery.go:35-44`）。
- 记录插入与用户额度增加在同一个数据库事务中，额度更新失败时记录会回滚（`model/lottery.go:125-147`）。
- 前端动画只展示服务器已经选定的结果，不能通过改动画或 board index 改变实际中奖奖项。

## 修复优先级

1. **立即处理：** 暂停或大幅降低默认奖项额度；增加全站每日预算、奖项库存和单次奖励上限，避免批量账号继续铸造额度。
2. **立即处理：** 增加抽奖专用反滥用策略：账号资格、验证条件、用户/IP/设备限流和异常账号封禁。
3. **高优先级：** 在服务端使用 `common.MaxQuota` 和严格边界检查，检查用户更新 `RowsAffected`，保证异常状态整笔回滚。
4. **高优先级：** 强制奖项位置与数量约束，补充并发冲突的 409 错误映射。
5. **中优先级：** 将奖励流水纳入可审计的事务模型，修复用户名脱敏和前端历史记录稳定 key。

## 总体判定

**不建议在未完成 P0/P1 修复前开放真实额度奖池。** 当前代码能够防住“同一账号同时点两次导致双倍发奖”这一类直接问题，但防不住“批量账号持续抽奖”和“高额默认奖池失控”这两类更现实、影响更大的额度滥用路径。
